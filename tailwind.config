import type { Config } from 'tailwindcss'

const config: Config = {
  content: [
    './pages/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
    './app/**/*.{js,ts,jsx,tsx,mdx}',
  ],

  theme: {
    extend: {

      // ── FONTS ──────────────────────────────────
      fontFamily: {
        syne:   ['Syne', 'sans-serif'],
        dm:     ['DM Sans', 'sans-serif'],
        sans:   ['DM Sans', 'sans-serif'],
        mono:   ['JetBrains Mono', 'Fira Code', 'monospace'],
      },

      // ── COLORS ─────────────────────────────────
      colors: {
        // Backgrounds
        bg:       '#0a0a0b',
        surface:  '#111113',
        surface2: '#17171a',

        // Borders
        border:   '#1e1e24',
        border2:  '#2e2e35',

        // Text
        primary:  '#f2ede8',
        muted:    '#888890',
        dim:      '#444450',

        // Accent
        accent:   '#e87c3e',
        accent2:  '#ff9d5c',

        // Service colors
        'ai-blue':     '#6fa3ef',
        'build-orange':'#e87c3e',
        'design-purple':'#b78bfa',
      },

      // ── SPACING ────────────────────────────────
      spacing: {
        '18': '4.5rem',
        '22': '5.5rem',
        '30': '7.5rem',
      },

      // ── BORDER RADIUS ──────────────────────────
      borderRadius: {
        'xl2': '1rem',
        'xl3': '1.25rem',
        'xl4': '1.5rem',
      },

      // ── LETTER SPACING ─────────────────────────
      letterSpacing: {
        tightest: '-0.05em',
        tighter:  '-0.03em',
        tight:    '-0.02em',
        wide:     '0.06em',
        wider:    '0.1em',
        widest:   '0.15em',
      },

      // ── LINE HEIGHT ────────────────────────────
      lineHeight: {
        tight:   '1.05',
        snug:    '1.15',
        normal:  '1.5',
        relaxed: '1.75',
      },

      // ── FONT SIZE ──────────────────────────────
      fontSize: {
        '2xs': ['10px', { lineHeight: '1.4' }],
        'xs':  ['11px', { lineHeight: '1.5' }],
        'sm':  ['13px', { lineHeight: '1.6' }],
        'base':['15px', { lineHeight: '1.7' }],
        'lg':  ['17px', { lineHeight: '1.4' }],
        'xl':  ['20px', { lineHeight: '1.3' }],
        '2xl': ['24px', { lineHeight: '1.2' }],
        '3xl': ['30px', { lineHeight: '1.1' }],
        '4xl': ['36px', { lineHeight: '1.05' }],
        '5xl': ['48px', { lineHeight: '1.0' }],
        '6xl': ['60px', { lineHeight: '0.97' }],
      },

      // ── BACKDROP BLUR ──────────────────────────
      backdropBlur: {
        nav: '12px',
      },

      // ── BACKGROUND IMAGE ───────────────────────
      backgroundImage: {
        'accent-glow': 'radial-gradient(ellipse, rgba(232,124,62,0.08) 0%, transparent 70%)',
        'hero-glow':   'radial-gradient(circle, rgba(232,124,62,0.06) 0%, transparent 70%)',
      },

      // ── ANIMATIONS ─────────────────────────────
      keyframes: {
        fadeUp: {
          '0%':   { opacity: '0', transform: 'translateY(24px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        fadeIn: {
          '0%':   { opacity: '0' },
          '100%': { opacity: '1' },
        },
        livePulse: {
          '0%, 100%': { opacity: '1' },
          '50%':      { opacity: '0.4' },
        },
        float: {
          '0%, 100%': { transform: 'translateY(0px)' },
          '50%':      { transform: 'translateY(-8px)' },
        },
        slideRight: {
          '0%':   { transform: 'translateX(-8px)', opacity: '0' },
          '100%': { transform: 'translateX(0)', opacity: '1' },
        },
      },

      animation: {
        'fade-up':    'fadeUp 0.6s ease forwards',
        'fade-in':    'fadeIn 0.5s ease forwards',
        'live-pulse': 'livePulse 2s infinite',
        'float':      'float 3s ease-in-out infinite',
        'slide-right':'slideRight 0.3s ease forwards',
      },

      // ── TRANSITIONS ────────────────────────────
      transitionDuration: {
        '150': '150ms',
        '250': '250ms',
        '350': '350ms',
        '400': '400ms',
      },

    },
  },

  plugins: [],
}

export default config